# TerminalAce
An open-source agent designed to crush Terminal-Bench Hard tasks.

## Getting Started
1. Build the sandbox image: `docker build -t terminal-ace-sandbox docker/`
2. Run agent: `python scripts/run_agent.py`